/** @deprecated Import from `./webhook` — kept for existing imports */
export {
  buildWebhookPayload,
  normalizeWebhookUrl,
  notifyWebhook as notifyMakeWebhook,
  type WebhookPayload as MakeWebhookPayload,
} from './webhook';
